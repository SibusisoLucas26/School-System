<!DOCTYPE html>
<html xmlns:th="http://www.thymeleaf.org">
<head>
    <meta charset="UTF-8">
    <title>Assignments List</title>
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.3.1/css/bootstrap.min.css">
    <link rel="stylesheet" th:href="@{/dash.css}">
</head>
<body>
<div class="container mt-5">
    <h1 class="mb-4">Assignments List</h1>
    <a href="/assignments/new" class="btn btn-primary mb-3">Add New Assignment</a>
    <a th:href="@{/teachers/dashboard}" class="btn btn-primary mb-4">Back to Dashboard</a>
    <table class="table">
        <thead>
        <tr>
            <th>ID</th>
            <th>Title</th>
            <th>Description</th>
            <th>Due Date</th>
            <th>Actions</th>
        </tr>
        </thead>
        <tbody>
        <tr th:each="assignment : ${assign}">
            <td th:text="${assignment.id}"></td>
            <td th:text="${assignment.title}"></td>
            <td th:text="${assignment.description}"></td>
            <td th:text="${assignment.dueDate}"></td>
            <td>
                <a th:href="@{/assignments/edit/{id}(id=${assignment.id})}" class="btn btn-primary">Edit</a>
                <a th:href="@{/assignments/delete/{id}(id=${assignment.id})}" class="btn btn-danger">Delete</a>
            </td>
        </tr>
        </tbody>
    </table>
</div>
<script src="https://code.jquery.com/jquery-3.3.1.slim.min.js"></script>
<script src="https://cdnjs.cloudflare.com/ajax/libs/popper.js/1.14.7/umd/popper.min.js"></script>
<script src="https://stackpath.bootstrapcdn.com/bootstrap/4.3.1/js/bootstrap.min.js"></script>
</body>
</html>
